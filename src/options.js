const devServer = require('./options/devServer');
const entry = require('./options/entry');
const output = require('./options/output');
const resolve = require('./options/resolve');

module.exports = {
    devServer,
    entry,
    output,
    resolve
};
